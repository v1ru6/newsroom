"""Ingestion adapters for raw external inputs.

RSS produces untrusted article items; KEV produces corroboration metadata. New
sources should normalize into the same models before reaching scoring.
"""
